RSpec.describe Fluent::Plugin::Wmi do
  it "has a version number" do
    expect(Fluent::Plugin::Wmi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
